%p#notice= notice
.container
  .page-header
    %h1 Booking History
.container
  %table.table.table-striped
    %thead
      %tr
        %th Movie
        %th Theater
        %th Show Date
        %th Show Time
        %th{:colspan => "3"}
    %tbody
      - @bookings.each do |booking|
        %tr
          %td= booking&.release&.movie&.name
          %td= booking&.release&.theater&.name
          %td= booking&.date
          %td= booking&.show_timing&.start_time.strftime('%I:%M %p')
          %td
            - unless booking&.past_show?
              = link_to 'Cancel', booking_path(booking), method: :delete,data: { confirm: 'Are you sure?' }
