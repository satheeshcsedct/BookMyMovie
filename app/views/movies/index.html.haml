%p#notice= notice
.container
  - @movies.each_slice(4) do |movie_objects|
    .row.p-3{styles: 'display: flex;flex-wrap: wrap;'}
      - movie_objects.each do |movie|
        = render partial: 'movie_card', locals: { movie: movie }
